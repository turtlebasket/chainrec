export type Entry = {
    ownerAddress: string,
    entryInfo: string,
    userInfo: string,
    entry: string,
    timestamp: number,
}

export type EntryAnon = {
    ownerAddress: string,
    entryInfo: string,
    entry: string,
    timestamp: number,
}
